import { VerticalBox } from "std-widgets.slint";

export component LivePage inherits Rectangle {
    background: #ffffff;

    VerticalBox {
        padding: 40px;
        spacing: 20px;
        alignment: start;

        Text {
            text: "live Content";
            font-size: 32px;
            color: #333333;
        }

     
    }
}